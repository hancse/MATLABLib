function table(X)%TABLE(X) displays the contents of matrix X (real elements only) in the Command Window%         as a table (tab-separated string with precision %13.6e). %         After marking the data it can tranferred to a spreadsheet by copy-and-paste.%           % Date 05-08-99% Written by Stefan Baunack. Please send any bug% reports or other comments to: s.baunack@ifw-dresden.de.if ~isreal(X), error('X must contain only real elements.'),endif isstr(X), error('X is a string matrix.'),end[nrow,ncol] = size(X);disp(' ')if ncol == 1,   sout = sprintf('%13.6e\n',X(:));   % create output string line by line at once   disp(sout)                         % display stringelse   for L = 1:nrow,      sout = [sprintf('%13.6e\t',X(L,1:ncol-1)) sprintf('%13.6e',X(L,ncol))];   % create output string for row L without trailing tab      disp(sout)                                                                % display string    endenddisp(' ')